# LVGL-AI-Lab CHANGELOG

## [Unreleased]
- 立项 + design v0 APPROVED + bootstrap 骨架
- v0.1 BACKLOG 重构为 2 张大卡（用户反馈"10 张拖沓"后简化）：Card 1 闭环调试系统 + Card 2 LVGL 表盘；卡内 sub-task 由 AI 自决
- KIT 资料解压 (D:\projects\GD\GD32F303RxT6 KIT\) + `docs/references/{INDEX, mcu, board, screens, sdk}.md` 资料库建立
- lessons/ + capability-report/ 目录预建（v0.1 retro 时填充）
- 工具链已钉死：CMake + arm-none-eabi-gcc + OpenOCD + GDB；烧录器 ST-Link；log USART1 PA9 TX 115200
- **Card 1 / st1**: pre/post 自评工具 `tools/session.sh` (new/close/list) + smoke gate `tests/smoke-test.js`（sessions 完整性检查阶段 1 启用，构建 size + 静态分析阶段 2/3 待激活）
- **Card 1 / st2**: CMake 工程从 KIT/Examples/RGB LED 抄出（vendor/gd32f30x + src + GCC 版 startup_gd32f303rc.S + linker/gd32f303rc.ld + cmake/arm-none-eabi.toolchain.cmake + 顶层 CMakeLists.txt）。`cmake --build build` 干净通过产 .elf/.bin/.hex/.map。size: Flash 1692B (0.83%) / RAM 3120B (7.62%)。smoke-test 阶段 2 (build size) 同步激活。
- **Card 1 / st3**: 烧录 + 调试 chain 通。pyOCD 0.44.0 装于 user 目录（替代 OpenOCD，pip 免 admin），ST-Link/V2 + target stm32f103rc 完美驱动 GD32F303RC SWD。`tools/flash.sh` 一键烧录 + `tools/gdb-server.sh` 启 GDB server :3333。烧录后 PC=0x0800030c / SP=0x2000c000=_estack 完美匹配 linker。references/INDEX.md 烧录段更新为 pyOCD 主路径。✅ 板上 RGB LED 1Hz 三色循环闪（用户视觉 sign-off）。
- **Card 1 / st4**: GDB attach + 断点 chain 通。`tools/gdb-init.txt` 非交互脚本：connect → reset halt → load → break main → stepi → detach。实测 break main 命中 src/main.c:16，PC=0x80002da, lr 链回 Reset_Handler+53，stepi 进 systick_config()。**load 段大小 sum = 1692 B 与 build size 字节级一致**，链路完整性双重验证。Card 1 主线（最小可闭环调试系统）✅ 完成。
- **Card 2 / sa**: LCD 1.47" ST7789V3 (320x172) bring-up。从 KIT/Examples/LCD 1.47inch 抄 spi BSP + tftlcd driver + tft_font 字模到 src/bsp/ + src/devices/lcd/。SPI2 默认引脚 (PA15/PB3/PB4/PB5/PB6/PB7) + 软件 NSS + Mode 3 + /8 prescaler。屏初始化 + 文字 + tick 心跳。Flash 25420B (9.70%)，RAM 4720B (9.60%)。references/board.md SPI 分配修正（之前误写 SPI0 remap）+ references/screens.md 1.47" 部分填实。中途用户澄清两次（IIC→SPI / 1.14→1.47），AI 切换路径 < 5 分钟。
- **Card 2 / sb**: LVGL v9.5.0 移植。external/lvgl 加为 git submodule pinned v9.5.0，src/lvgl_port/{lv_conf.h, lv_port_disp.c} 桥接 LVGL flush_cb 到 KIT tftlcd 的 LCD_Address_Set + SPI2_WriteBytes，SysTick handler 加 lv_tick_inc(1)。lv_conf 关 theme_default + draw_sw_complex 省 41KB，最终 Flash 224384B / 256K (87.65%)。修了 RGB565 字节序坑（LVGL 内存 LE / ST7789 期望 BE，用 lv_draw_sw_rgb565_swap 原地 swap）。3 步硬件诊断（LED toggle → LCD_Fill 红色 area → 字节序 swap）定位到根因。smoke-test Flash 阈值 200K → 240K + 注释历史。屏上工作：静态 banner + 动态 100ms tick label + lv_bar 进度条循环。用户视觉 sign-off ✅。
- **Card 2 / sb 后期 + sc**: 启 sysmon (PERF/MEM overlay)、加 spinner、接 RTC 时间显示。期间走了 6 回合 debug：(1) PERF_MONITOR 不显示 → 漏开 LV_USE_SYSMON；(2) sysmon 与 banner 撞车 → 改单行 banner；(3) spinner 不显示 → 重开 LV_DRAW_SW_COMPLEX（spinner=arc 依赖它）；(4) 用户 "banner 残影" 诊断 3 次错方向（实为字 AA 边缘视觉，不是 GRAM 残留）；(5) widget 全开后 LV_MEM_SIZE=8K OOM → TLSF 死循环（GDB 抓到），改 16K；(6) 删 banner 收尾。最终配置：LV_DRAW_SW_COMPLEX=1, LV_USE_SYSMON=1, LV_MEM_SIZE=16K。Flash 242K (101% of 240K 阈值，需后续微调)，RAM 29K (60% of 48K)。RTC: src/bsp/rtc.{c,h} LSE 32.768kHz + BKP + 1Hz prescaler，时间从 12:00:00 走起。**全屏 FPS 实测**: 30 @ 120×120 spinner = 864 KB/s SPI 吞吐 = 92% utilization；整屏 110KB 推算 ~8.5 FPS 物理上限。元反思（5 次 debug 失败的共同模式 = "局部解决而忘检全局"）记入 sc post + 待 retro 抽 lesson。
- **Card 2 / sd**: 仿华为 GT3 表盘 layout 上线。Figma-spec 文档 (`docs/designs/2026-05-02-watchface-spec-v1.md`) 含每元素精确标注（color tokens / typography / 绝对坐标）。布局：顶日期 / 左玫瑰金步数 ring / 中央时间 card（120×40 玫瑰金边框 + 14px 字 framing 提升视觉权重）/ 右绿色心率 ring / 底蓝色秒条。配置变化：关 LV_USE_SYSMON 节省 6KB Flash；LV_DRAW_LAYER_SIMPLE_BUF_SIZE 24K → 8K（避免 transform layer 路径的 size 不一致）。**大字时间 4 次实验全失败**（Montserrat 24/28 +25-33KB 超 256K 上限；transform_scale 2.0× 在 LVGL v9.5 partial mode 下触发"屏只显示一半" bug，4 次假设[layer alloc OOM / chunk size / SIMPLE_BUF_SIZE / px_map stride]全验错），按 LTRYSL BFS backtrack 到 14px + framing 路径。最终 Flash 240980 B (91.93% of 250K)。仿 GT3 layout 视觉 sign-off ✅。lesson 候选：LVGL v9 transform_scale + partial buffer 不兼容 / 嵌入式 GUI 大字渲染选项谱。元命题升级：design.md 加 v0.2 hands-off 北极星 + 屏视觉自动验收 (GDB 读 partial buf + pixel hash) 作为消除 user-in-the-loop 关键路径；card2 plan 从 weekly 改 daily MVP 颗粒度（应用 LTIRB3）。
- **Card 3 / sj**: 大字字体从 SD 加载通过——突破 256K Flash 字体上限。用 `npx lv_font_conv 1.5.3 --font Montserrat-Medium.ttf --size 28 --bpp 4 --range 0x20-0x7F --format bin` 离线生成 16480B 字体，**dogfood v0.2 hands-off 北极星**：嵌入 blob 到固件 (montserrat_28_blob.c)，首次启动 `install_font_if_missing()` 用 f_write 自装到 SD，下次启动 idempotent 跳过。零用户拆卡介入 = automation 一阶段实证。**坑 1**：第一次 lv_binfont_create 返 NULL，看似格式不兼容；加 hex dump 诊断显 `30000000|head|01000000` 正是 v9.5 期望 → 反推 LV_MEM_SIZE=16K 不够，binfont 内部 lv_malloc 静默 fail。升 24K 后通过。**坑 2**：lv_label_set_text_fmt 在 OOM 边缘静默失败，label 空白，改用 `sprintf+set_text`。**多论据 sign-off 栈穿透**：sg (SD identification) → sh (FATFS read) → sj (FATFS write + binfont parse + LVGL render) 三层链 — sd_disk_write 数据路径首次端到端验证（CMD24 + DMA 写）。Flash 250692B (95.63%) RAM 39336B (80%)。Lesson 候选：LXO38R 第 4 次重现（LV_MEM_SIZE 也是 lv_conf 隐式约束）+ "binfont FAIL 先查 LV_MEM_SIZE 而非格式" + "嵌入自装 = hands-off 资源安装模式"。
- **Card 3 / si**: LVGL `lv_fs` 桥到 FatFs，"S:" drive 端到端通。启 `LV_USE_FS_FATFS=1` + `LV_FS_FATFS_LETTER='S'` + `LV_FS_FATFS_PATH="0:"`，调 `lv_fs_fatfs_init()` 注册（无需自写 glue）。**Flash 大坑**：第一次 270K 超 14K → 关 21 个未用 LVGL widget（CALENDAR/CHART/SPINBOX/TABVIEW/CANVAS/SCALE/MENU/...）+ LOG_LEVEL=NONE + OBJ_PROPERTY_NAME=0 → 227K (88.21%)。**LXO38R 第 3 次重现**：gc-sections 删不掉 widget（dispatch table / theme bind 拉它们进 .text），必须用 `LV_USE_*=0` 编译期裁剪。**额外坑**：Release `-Os` 反而比 Debug `-Og` 大（343K vs 270K，同 source），机制未深究，继续 Debug。**lv_fs_open 返 ERR 13 (UNKNOWN) 排查**：(1) 先猜 FF_USE_LFN=0 让 f_open fail（开 LFN 仍 NO_FILE）→ (2) 加 direct f_open 排除 lv_fs glue → (3) 加 readdir 列根目录 6 文件 → **真因：根目录已无 README.TXT**（sh 时存在，si 不在；环境状态丢失）。修法：改"读 readdir 返回的第一个 regular file"绕掉文件名假设。多论据 sign-off：picked 文件 + lv_fs_open=OK + lv_fs_read 内容显示 + tick label 递增。Flash 231K (88.21%) RAM 30K (63%)。
- **Card 3 / sh**: FatFs R0.13c 挂载 + 读文件成功。从 KIT/Utilities/Third_Party/fat_fs/ 抄 12 个文件到 `external/fatfs/`，写 `src/devices/sd/sd_diskio.{c,h}` 实现 5-callback Diskio_drvTypeDef（alignment-aware bounce buffer），ffconf.h 裁剪（关 EXFAT/MKFS、CODE_PAGE 932→437、_VOLUMES 2→1）。**意外坑**：第一次 mount=1 (FR_DISK_ERR)，三步硬件诊断（先 raw `sd_block_read(s0)` 隔离）→ `rd=CMD_TIMEOUT`，定位到数据路径。**根因**：sg 调的 `sd_init()` 只完成 identification，KIT example main.c 的 `sd_io_init` wrapper 还有 3 步（CMD7 select_card → bus_mode_config 4-bit → transfer_mode_config polling）漏掉。修这 3 步后 raw read OK + mount=0 + f_open=0 + 文件内容显示。Flash 41160B (15.69%) RAM 7.6K (15.92%)。**重要 lesson**：sg "SD OK" sign-off 是假阳性 — 4 个论据（Type/Cap/RCA/CID）全在 identification 层，没穿透 CMD17 数据路径；多论据原则 LTIRBE 反例。Lesson 候选：vendor lib 完整 init recipe 必从 Examples/main.c 反推（LXO38R 第 2 次重现）。
- **Card 3 / sg**: SDIO bring-up 一次跑通。从 KIT/Examples/SDIO/Read_write 抄 `sdcard.{c,h}` (2448+262 行完整 SD protocol stack: CMD0/8/2/3/9/7/55/41 + ACMD41 + CSD parse + block r/w) 到 `src/devices/sd/`。**关键发现**：sdcard.c **self-contained**（内含 RCU + GPIO PC8-12+PD2 AF_PP 50MHz + SDIO 24MHz prescaler + polling mode），用户层零适配。原 plan 6 步（含 sd_gpio.c）压到 3 步（抄文件 + 改 main.c + CMake include）。main.c 暂删 LVGL 改写为 SD test：`sd_init() → sd_card_information_get(&info) → sd_card_capacity_get()` → 屏显 Type/Capacity/RCA/CID。Flash 28436B (10.85%)，RAM 5232B (10.64%)。**多论据 sign-off**：单看 "SD OK!" 不够（CMD0 假阳性可能），同步显示 CID/RCA/Capacity 4 个独立 datapoint = identification phase 真完成。剩 228K Flash 给 FATFS + LVGL + GUI 资源，验证 v0.2 北极星"SD 突破 256K"路径可行。
- **Card 2 / sf-retro**: v0.1 收口。从 9 个 sub-task pre/post（18 sessions 文件）抽：5 项目 lessons（LVGL v9 size trim / transform partial broken / lv_conf deps / Keil-GCC startup / pyOCD GD32 兼容）+ 2 跨项目 meta lessons（局部 vs 全局配置纪律 / 嵌入式硬件三步诊断法）通过 sediment skill 批量生成。**capability-report v0.1** (`docs/capability-report/v0.1.md`) 含 4 层完成度（infra/bring-up/LVGL/调试方法论）+ 13 次用户介入统计 + FPS 多论据三角验证 + 实际 10h vs design 5-6 周 = 30-50× AI 时间压缩比 datapoint。**qa-report** GT3 watchface 视觉 spec 9/9 PASS。**v0.2 Card 3 plan 已开**：SD 卡 + FATFS + GUI 资源动态加载（突破 256K Flash 限制 + 大字字体）。state.md 推到 idle, v0_1_done=true。
